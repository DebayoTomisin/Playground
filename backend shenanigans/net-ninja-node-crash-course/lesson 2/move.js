const name = "bola";

// console.log(name);

// console.log(global);

// console.log(__dirname); // an absolue path of the directory we are in
// console.log(__filename); // an absolue path of the file we are in

const xyz = require("./module.js");

console.log(xyz);
