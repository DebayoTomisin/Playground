* node wraps v8 engine. V8 engine basically runs on the browser.

The role of node is to communicate responses to request from frontend application

The Global object in node is similar to the the window object inside a browser, it basically host a ton of methods and functions.